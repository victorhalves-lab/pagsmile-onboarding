{
  "name": "ComplianceConfig",
  "type": "object",
  "properties": {
    "configKey": {
      "type": "string",
      "description": "Chave única da configuração (ex: face_match_threshold, rejection_reasons_visible)"
    },
    "configValue": {
      "type": "string",
      "description": "Valor da configuração (string, será parseado conforme o tipo)"
    },
    "configType": {
      "type": "string",
      "enum": [
        "number",
        "boolean",
        "string",
        "json"
      ],
      "default": "string",
      "description": "Tipo do valor para parsing"
    },
    "category": {
      "type": "string",
      "enum": [
        "CAF",
        "BDC",
        "RISK_SCORING",
        "REGULATORY",
        "GENERAL"
      ],
      "description": "Categoria da configuração"
    },
    "description": {
      "type": "string",
      "description": "Descrição legível da configuração"
    },
    "isActive": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "configKey",
    "configValue",
    "category"
  ]
}
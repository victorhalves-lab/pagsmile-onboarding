import React from 'react';
import { Sec, H1, P, Note } from '../DocPrimitives';

/**
 * Capítulo 2 — Glossário Técnico Microscópico
 *
 * STUB temporário. O conteúdo EXTREMO completo será produzido no próximo turno
 * (ver plano de capítulos no orquestrador). Este stub existe apenas para destravar
 * o build enquanto o conteúdo é escrito sub-componente por sub-componente.
 */
export default function Ch02_Glossario() {
  return (
    <Sec id="ch-02">
      <H1 num="02">Glossário Técnico — Termos, Siglas e Identificadores</H1>

      <Note title="Capítulo em produção" kind="info">
        Este capítulo está sendo refatorado para o padrão EXTREMO microscópico — cobrindo cada sigla, cada constante, cada enum, cada threshold da plataforma.
        <br/>
        Conteúdo completo será publicado no próximo turno seguindo o mesmo padrão do Capítulo 1 (sub-componentes focados em <C>chapters/ch02/</C>).
      </Note>

      <P>Conteúdo em construção.</P>
    </Sec>
  );
}
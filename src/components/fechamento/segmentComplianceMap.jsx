/**
 * Mapeamento de nome de segmento (landing page) → modelo de compliance (ComplianceDinamico ?model=)
 * Usa segmentConfig como fonte de verdade.
 */
import { SEGMENT_LABEL_TO_COMPLIANCE } from '@/lib/segmentConfig';

const SEGMENT_TO_COMPLIANCE = SEGMENT_LABEL_TO_COMPLIANCE;

export default SEGMENT_TO_COMPLIANCE;